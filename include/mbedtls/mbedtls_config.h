#ifndef MBEDTLS_CONFIG_H
#define MBEDTLS_CONFIG_H

// Project-owned configuration selected ahead of the unmodified vendored
// default through the application's include-path ordering.
#include "doodle_mbedtls_config.h"

#endif
